
import { TravelMode, TripPurpose, TripFrequency } from './types';

export const TRAVEL_MODES = Object.values(TravelMode);
export const TRIP_PURPOSES = Object.values(TripPurpose);
export const TRIP_FREQUENCIES = Object.values(TripFrequency);
